# digital-time-capsule
The Digital Time Capsule is a Python-based security tool that encrypts user messages and locks them until a specific future date. Using the Fernet AES-256 algorithm, it ensures data privacy by preventing decryption until the system clock matches the unlock date. It features a CLI for sealing and opening capsules with automated key management.
